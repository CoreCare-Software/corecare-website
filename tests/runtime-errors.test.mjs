import test from 'node:test';import assert from 'node:assert/strict';import { readFile } from 'node:fs/promises';
test('website Worker persists privacy-safe server diagnostics',async()=>{const source=await readFile(new URL('../worker/index.ts',import.meta.url),'utf8'),helper=await readFile(new URL('../worker/runtime-errors.ts',import.meta.url),'utf8'),migration=await readFile(new URL('../drizzle/0005_durable_runtime_errors.sql',import.meta.url),'utf8');assert.match(source,/await recordRuntimeError\(env,/);assert.match(migration,/CREATE TABLE IF NOT EXISTS api_error_log/);assert.match(helper,/\[redacted\]/);assert.doesNotMatch(source,/recordRuntimeError\([^)]*(?:request\.body|requestBody|body:)/s);});
