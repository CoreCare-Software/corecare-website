# CoreCare Platform 1.0.0

Initial dedicated CoreCare software-owner command centre, migrated from the owner workspace previously embedded in CoreCare Care.

No new database migration is required because Platform uses the existing CoreCare D1 database and existing platform tables.
