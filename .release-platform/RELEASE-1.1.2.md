# CoreCare Platform 1.1.2

Fixes platform workspace view visibility. Command Centre dashboard layout rules could override the HTML hidden attribute, leaving its cards visible when Ecosystem Control was selected. Explicit high-priority hidden rules now ensure each platform navigation item displays only its own workspace.
