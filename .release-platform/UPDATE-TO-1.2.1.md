# Update CoreCare Platform to 1.2.1

Copy the changed files into the existing corecare-platform repository and replace the destination files.

Run:

```cmd
npm install
npm run check
npm run deploy
```

No D1 migration is required. After deployment, refresh with Ctrl + F5.
