# CoreCare Platform v1.4.2 — Support Centre Stability

## Fixes

- Adds the missing Support Centre KPI elements expected by the browser application.
- Adds the missing assignee filter used by the ticket renderer.
- Makes all support KPI updates null-safe.
- Keeps the ticket queue working when optional ticket data is absent.
- Provides a clear empty state when no support tickets have been submitted.
- Updates the Platform version and browser cache key to 1.4.2.

## Database

No database migration is required.
