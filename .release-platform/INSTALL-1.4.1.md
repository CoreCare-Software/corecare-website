# Install CoreCare Platform 1.4.1

Copy everything inside this `corecare-platform` folder over the existing repository folder and replace files.

Run:

```cmd
npm install
npm run check
npm run deploy
```

No database migration is required.
