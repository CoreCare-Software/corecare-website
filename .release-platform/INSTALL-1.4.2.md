# Install CoreCare Platform 1.4.2

Copy the contents of this corecare-platform folder over the existing repository and replace matching files.

Run:

```cmd
npm install
npm run check
npm run deploy
```

No database migration is required. Refresh the deployed site with Ctrl + F5.
