# Update to 1.3.0

Copy the changed files into the repository, then run:

```cmd
npm install
npm run check
npm run db:migrate:remote
npm run deploy
```

Approve migration `0041_support_and_organisation_operations.sql`.
