#!/usr/bin/env node
import{spawnSync}from'node:child_process';import path from'node:path';import process from'node:process';import{fileURLToPath}from'node:url';if(process.env.CI==='true'||process.env.GITHUB_ACTIONS==='true')process.exit(0);const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'..'),git=a=>spawnSync('git',a,{cwd:root,encoding:'utf8'}),r=git(['rev-parse','--show-toplevel']);if(r.status!==0||path.resolve(r.stdout.trim()).toLowerCase()!==root.toLowerCase())process.exit(1);if(git(['config','--local','core.hooksPath','.githooks']).status!==0)process.exit(1);const v=git(['config','--local','--get','core.hooksPath']);if(v.status!==0||v.stdout.trim()!=='.githooks')process.exit(1);console.log('PASS CoreCare hooks installed: core.hooksPath=.githooks');
