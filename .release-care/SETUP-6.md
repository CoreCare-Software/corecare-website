# Sprint 6 deployment

1. Replace the project files with this release.
2. Run `npx.cmd wrangler d1 migrations apply corecare --remote`.
3. Run `npm.cmd run check`.
4. Commit and push to GitHub.
5. Hard refresh CoreCare and test with fictional data only.
