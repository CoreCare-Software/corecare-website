# Sprint 8.2 — SaaS platform controls

Adds audited Platform Owner support mode, return-to-platform controls, organisation branding/contact settings, subscription metadata and feature-limit foundations. Organisation users remain tenant-isolated and never receive platform navigation or platform API access.

Apply migration `0008_saas_platform_controls.sql` before deployment.
