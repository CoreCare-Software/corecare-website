# CoreCare Enterprise 1.5.0 — Electronic Call Monitoring Foundation

Adds scheduled visits, secure client visit codes, clock-in/out events, live visit statuses, offline device queue, automatic retry, duplicate protection and audit logging.

## Deployment
Database migration required: Yes (`0022_electronic_call_monitoring.sql`).
