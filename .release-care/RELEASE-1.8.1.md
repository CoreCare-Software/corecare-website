# CoreCare Enterprise 1.8.1 — Scheduling Board Polish

- Always renders the scheduling timeline, even before staff or visits exist.
- Adds guided empty rows and an Add care worker action instead of a blank canvas.
- Extends the planning day from 06:00 to 22:00.
- Makes the allocation queue compact and internally scrollable.
- Improves date navigation, sticky headers, grid density and responsive behaviour.
- Updates all visible and internal version references to 1.8.1.

No database migration is required.
