# CoreCare Enterprise v1.25.1

Care-plan domain toggle stability hotfix.

- Replaces the browser `hidden` property with dedicated CSS classes.
- Prevents the care-plan form from disappearing when a domain is turned off.
- Adds independent expand/collapse behaviour for enabled domains.
- Updates visible deployment version and asset cache keys.

No database migration is required.
